---
import BlogPreview from '../components/BlogPreview.astro';
import Carousel from '../components/Carousel.astro';
import Contact from '../components/Contact.astro';
import CookieConsent from '../components/CookieConsent.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Partners from '../components/Partners.astro';
import Products from '../components/Products.astro';
import Layout from '../layouts/Layout.astro';
// Pokud používáte content collections, je potřeba přidat konfiguraci
import { getCollection } from 'astro:content';

// Získání blogových příspěvků pro BlogPreview
const allPosts = await getCollection('blog');
const images = [
  { src: '/images/uploads/banner-test.jpg', alt: 'First placeholder' },
  { src: '/images/uploads/banner-test2.jpg', alt: 'Second placeholder' },
  { src: '/images/uploads/banner-test.jpg', alt: 'Third placeholder' },
];
const carouselSlides = [
  {
    src: "/images/uploads/banner-test.jpg",
    alt: "První obrázek",
    html: `
      <style>
        @media (max-width: 480px) {
          .carousel-text-container {
            bottom: 15% !important;
            left: 5% !important;
          }
          .carousel-text-container h2 {
            margin-bottom: 5px !important;
          }
        }
      </style>
      <div class="carousel-text-container" style="position: absolute; bottom: 18%; left: 9.1%; color: white; max-width: 80%; padding: 0; line-height: 1.1;">
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">Hubněte</h2>
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">moderně!</h2>
        <hr style="margin: clamp(0.8rem, 2vw, 3.5rem) 0 clamp(0.5rem, 1vw, 10px) 0; border: none; border-top: clamp(2px, 0.5vw, 6px) solid white; width: clamp(80px, 40%, 200px); margin-left: 0;">
        <h2 style="margin: clamp(0.8rem, 2vw, 2.5rem) 0 10px 0; font-size: clamp(1.2rem, 4vw, 4rem);">gastro balonky</h2>
      </div>
    `
  },
  {
    src: "/images/uploads/banner-test3.jpg",
    alt: "Druhý obrázek",
    html: `
      <style>
        @media (max-width: 480px) {
          .carousel-text-container {
            bottom: 15% !important;
            left: 5% !important;
          }
          .carousel-text-container h2 {
            margin-bottom: 5px !important;
          }
        }
      </style>
      <div class="carousel-text-container" style="position: absolute; bottom: 18%; left: 9.1%; color: white; max-width: 80%; padding: 0; line-height: 1.1;">
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">Hubněte</h2>
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">moderně!</h2>
        <hr style="margin: clamp(0.8rem, 2vw, 3.5rem) 0 clamp(0.5rem, 1vw, 10px) 0; border: none; border-top: clamp(2px, 0.5vw, 6px) solid white; width: clamp(80px, 40%, 200px); margin-left: 0;">
        <h2 style="margin: clamp(0.8rem, 2vw, 2.5rem) 0 10px 0; font-size: clamp(1.2rem, 4vw, 4rem);">gastro balonky</h2>
      </div>
    `
  },
  {
    src: "/images/uploads/banner-test.jpg",
    alt: "Třetí obrázek",
    html: `
      <style>
        @media (max-width: 480px) {
          .carousel-text-container {
            bottom: 15% !important;
            left: 5% !important;
          }
          .carousel-text-container h2 {
            margin-bottom: 5px !important;
          }
        }
      </style>
      <div class="carousel-text-container" style="position: absolute; bottom: 18%; left: 9.1%; color: white; max-width: 80%; padding: 0; line-height: 1.1;">
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">Hubněte</h2>
        <h2 style="margin: 0 0 10px 0; font-size: clamp(1.5rem, 6vw, 5.8rem); font-weight: bold;">moderně!</h2>
        <hr style="margin: clamp(0.8rem, 2vw, 3.5rem) 0 clamp(0.5rem, 1vw, 10px) 0; border: none; border-top: clamp(2px, 0.5vw, 6px) solid white; width: clamp(80px, 40%, 200px); margin-left: 0;">
        <h2 style="margin: clamp(0.8rem, 2vw, 2.5rem) 0 10px 0; font-size: clamp(1.2rem, 4vw, 4rem);">gastro balonky</h2>
      </div>
    `
  }
];
---

<Layout title="Superior Medical - Špičkové zdravotnické pomůcky">
  <Header />
  <main>
    <CookieConsent />
    <Carousel slides={carouselSlides} id="main-carousel"/>
    <Hero />
    <Products />
    <BlogPreview posts={allPosts} />
    <Partners />
    <Contact />
  </main>
  <Footer />
</Layout>
