<!-- ImageCarousel.astro -->
---
interface CarouselSlide {
  src: string;
  alt: string;
  html?: string; // HTML obsah, který se přímo vloží do slidu
}

const {
  slides = []
} = Astro.props;
---

<div class="carousel-container">
  <div class="swiper">
    <div class="swiper-wrapper">
      {slides.map((slide: CarouselSlide) => (
        <div class="swiper-slide">
          <div class="slide-wrapper">
            <img src={slide.src} alt={slide.alt} />
            {slide.html && (
              <div class="slide-content" set:html={slide.html} />
            )}
          </div>
        </div>
      ))}
    </div>
    <div class="swiper-pagination"></div>
    <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div>
  </div>
</div>

<style>
.carousel-container {
  width: 100vw;
  margin-left: calc(-50vw + 50%);
  position: relative;
  overflow: hidden;
}
.swiper {
  width: 100%;
}
.slide-wrapper {
  position: relative;
  width: 100%;
  height: 100%;
}
.slide-wrapper img {
  display: block;
  width: 100%;
  height: auto;
  object-fit: cover;
}
.slide-content {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none; /* Umožňuje klikání na ovládací prvky karuselu */
}
/* Děti .slide-content mohou mít pointer-events zpět zapnuté */
.slide-content > * {
  pointer-events: auto;
}
.swiper-button-next,
.swiper-button-prev {
  color: white;
  filter: drop-shadow(0px 0px 3px rgba(0, 0, 0, 0.7));
  z-index: 20; /* Zajistí, že tlačítka budou nad obsahem slidu */
}
.swiper-pagination {
  z-index: 20; /* Zajistí, že pagination bude nad obsahem slidu */
}
.swiper-pagination-bullet {
  background: white;
  opacity: 0.7;
}
.swiper-pagination-bullet-active {
  opacity: 1;
}
@media (max-width: 768px) {
  .swiper-button-next,
  .swiper-button-prev {
    display: none;
  }
}
</style>

<script>
import Swiper from 'swiper';
import { Navigation, Pagination, Autoplay, EffectFade } from 'swiper/modules';
import 'swiper/css';
import 'swiper/css/navigation';
import 'swiper/css/pagination';
import 'swiper/css/effect-fade';

function initSwiper() {
  const swiper = new Swiper('.swiper', {
    modules: [Navigation, Pagination, Autoplay, EffectFade],
    slidesPerView: 1,
    spaceBetween: 0,
    loop: true,
    autoplay: {
      delay: 500000,
      disableOnInteraction: false,
    },
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
    effect: 'fade',
    fadeEffect: {
      crossFade: true
    },
  });
}

document.addEventListener('DOMContentLoaded', initSwiper);
document.addEventListener('astro:page-load', initSwiper);
</script>

<!-- Příklad použití: -->
<!--
---
import ImageCarousel from '../components/ImageCarousel.astro';

const carouselSlides = [
  { 
    src: "/images/uploads/image1.webp", 
    alt: "První obrázek",
    html: `
      <div style="position: absolute; bottom: 50px; left: 50px; background-color: rgba(0,0,0,0.7); color: white; padding: 20px; max-width: 400px; border-radius: 5px;">
        <h2 style="margin: 0 0 10px 0; font-size: 24px;">Nadpis prvního slidu</h2>
        <p style="margin: 0; font-size: 16px;">Toto je popis prvního slidu. Text můžete libovolně stylovat a umisťovat.</p>
        <a href="/odkaz" style="display: inline-block; margin-top: 15px; background: #007BFF; color: white; padding: 8px 15px; text-decoration: none; border-radius: 4px;">Více informací</a>
      </div>
    `
  },
  { 
    src: "/images/uploads/image2.webp", 
    alt: "Druhý obrázek",
    html: `
      <div style="position: absolute; top: 30px; right: 30px; background-color: rgba(255,255,255,0.8); color: black; padding: 15px; max-width: 300px;">
        <h2 style="margin: 0; font-size: 20px; color: #333;">Jinak umístěný text</h2>
        <p style="margin: 5px 0 0 0;">S jiným stylem a barvou pozadí.</p>
      </div>
    `
  },
  { 
    src: "/images/uploads/image3.jpg", 
    alt: "Třetí obrázek",
    html: `
      <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); text-align: center; background-color: rgba(0,0,0,0.5); color: white; padding: 30px; border-radius: 10px;">
        <h2 style="margin: 0; font-size: 32px; font-weight: 700;">Uprostřed obrázku</h2>
        <p style="margin: 10px 0 0 0; font-size: 18px;">Text uprostřed s možností přidat i další HTML prvky.</p>
      </div>
    `
  }
];
---
<ImageCarousel slides={carouselSlides} />
-->